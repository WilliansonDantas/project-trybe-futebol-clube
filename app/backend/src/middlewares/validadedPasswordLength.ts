// const lengthFive = 5;

// const validadedPasswordLength = (req, res, next) => {
//     const { password } = req.body;
//     if (password.length <= lengthFive) {
//         return res.status(400).json({ message: 'O "password" deve ter pelo menos 6 caracteres' });
//     }
//     next();
// };

// module.exports = validadedPasswordLength;
